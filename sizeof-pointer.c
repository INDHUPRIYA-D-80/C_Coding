//write a program to declare a character pointer to a variable and print the size of variable and size of ponter variable
#include<stdio.h>
int main()
{
    char a;
    int *ptr;
    printf("%d %d",sizeof(a),sizeof(ptr));
}
